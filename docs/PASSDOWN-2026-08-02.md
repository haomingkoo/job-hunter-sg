# Recruitment V4 continuation status

Updated 2026-08-03. This replaces the stale 2026-08-02 passdown state; it is a current
continuation guide, not a historical list of defects that have already been fixed.

## Current outcome

The coordinator supports real multi-turn work, automatic cached resume study, evidence-safe
resume edits, repair-and-rejudge assessment, readable replies, logical resume blocks, and
source-backed job facts. The ranked-match branch completes #140, #142, #143, and #148;
#144 and #147 remain after that branch lands.

## Landed changes

| PR | Current behavior |
|---|---|
| 149 | AI readiness probes the configured model. |
| 151 | Coordinator chat turns use the deep-agent tool loop. |
| 152 | Resume block IDs reach the edit tools. |
| 153 | Read endpoints avoid live-model construction; invalid preferences no longer discard turns; prose-only model answers complete without a 503; dead prompts were removed. |
| 154 | Score explanations can cite their score and responses have sufficient token budgets. |
| 156 | Accepted resume edits continue the active thread. |
| 157 | Resume-version studies start automatically and reuse completed checkpoints. |
| 158 | Repairable assessments are corrected and judged again instead of being discarded. |
| 159 | Long assistant replies are paragraphized for display. |
| 160 | Wrapped source lines migrate into logical resume blocks without splitting sentences. |
| 161 | Privacy-safe operational traces persist tool linkage, duration, model, and failure metadata. |
| 162 | Automatic-study activity reaches the live panel; cached profiles resolve without rerunning. |
| 163 | Search results expose parsed requirements, ATS terms, sector, seniority, and descriptive salary context without imputing missing pay. |

## Ready to merge

The `feat/recruitment-ranked-shortlist` branch adds one evidence-gated publication seam:

- `record_preferences` persists independently retractable candidate preferences from exact
  quotes in the latest message; withdrawals must identify an existing stored fact.
- `write_shortlist` publishes an ordered subset of jobs the coordinator actually read.
  Every card carries matched, stretch, and missing evidence, a verbatim resume quote, and
  separate level/pay judgments. Unknown jobs, duplicate jobs, invalid quotes, and salary
  invention are rejected.
- A new useful search clears explanations from the prior result set so stale rationales do
  not appear beside different jobs.
- The activity stream reports preference capture and shortlist publication accurately; it
  no longer calls a published shortlist a resume edit.

Full local verification on 2026-08-03: 940 backend tests passed (4 skipped), 188 frontend
tests passed, and the production frontend built. In a browser-backed two-turn run, the
coordinator recorded three preferences separately, excluded computer-vision and entry-level
postings, ranked SAP's $8,000 floor above lower-paid peers, labelled EY below peer median,
and rendered three evidence-cited cards. “Actually, I am open to computer vision now” then
removed only that stored constraint, retained the senior-IC and no-entry-level preferences,
and returned computer-vision roles.

## Verified user paths

- A real three-turn API conversation searched current jobs, honoured a pivot from
  management to individual-contributor roles, explained the fit from resume evidence, and
  persisted eight pending edits after `just finish it for me`.
- The V3 assessment pipeline completed with four specialist roles, an independent judge,
  29 criteria, seven recommendations, and five profile fields.
- A browser-driven fresh thread showed the automatic candidate profiler within about one
  second while the coordinator continued independently. The completed artifact was reused
  by a second thread with no new study run.
- The resume migration converted the 72-line acceptance fixture into 48 logical blocks.

## Active work

1. Merge the ranked-match branch and close #140, #142, #143, and #148 with its test and
   browser evidence.
2. Complete #144 and #147 acceptance behavior.
3. Audit tests and documentation for destructive revise behavior,
   fragmented block expectations, false persona claims, and obsolete test counts.
4. Run the full backend/frontend suites and repeat the live browser workflow before closing
   the V4 epic.

## Commands

```bash
PYTHONPATH=backend backend/.venv/bin/python -m pytest backend/tests tests -q
cd frontend && npm test

cd backend
set -a && . ./.env && set +a
PORT=8010 .venv/bin/python -u main.py

cd frontend
VITE_API_URL=http://localhost:8010 npm run dev
```

For a model-backed coordinator trace, work on a copy of the database:

```bash
PYTHONPATH=backend backend/.venv/bin/python backend/scripts/trace_coordinator_turn.py \
  --resume tmp/pdfs/haoming-ai-ml-manager/resume.txt \
  --corpus <copy-of-jobhunter.db> \
  --message "ok i need help with my resume"
```

## Durable cautions

- A green scripted suite does not prove model stopping, tool selection, streaming, or
  browser-visible behavior. Verify those paths directly.
- When a tool refuses an action, return the actionable alternative or missing identifiers;
  an identical refusal can cause the model to retry until its cap.
- Keep model work outside the long-held conversation lock. Persisted thread events may
  take the lock briefly to allocate their ordered sequence.
- Do not add a second persona pipeline to satisfy old copy. Use the existing candidate
  profiler, coordinator tools, and target-assessment specialists at their actual seams.
- Do not preserve tests that assert superseded behavior. Replace them with coverage for
  the current contract, while retaining evidence and safety gates.
- Never run mutation-heavy trace scripts against the primary local database.

## Product invariant

The assistant may autonomously search, compare, explain, and draft within the candidate's
job hunt. Resume claims and edits must remain supported by the candidate's resume and the
job evidence. The candidate can interrupt, change direction, ask why, or ask the team to
finish the work without losing accepted evidence or pending edits.
