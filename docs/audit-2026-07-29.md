# Audit findings — 2026-07-29

Three adversarially-verified audit workflows (100 agents total) plus a live browser UAT.
Every finding below was independently re-checked by a second agent instructed to refute it;
refuted claims are excluded. Severity is the verifier's corrected severity where it differs
from the original reviewer's.

## Job feed / balanced sort

19 confirmed, 8 refuted.

| Severity | Area | Location | Finding |
|---|---|---|---|
| blocker | sql-correctness | `backend/main.py:5009` | The per-company cap is applied to filtered/keyword searches, not just the browse feed — searching a company returns at most 3 jobs and `total` reports 3 |
| blocker | test-gaps | `backend/tests/test_job_feed_diversity.py:91` | No test asserts `total` or cross-page reachability — the cap silently deletes rows from the whole corpus, not just page 1 |
| blocker | test-gaps | `backend/tests/test_job_feed_diversity.py:87` | No test combines `balanced` with `q` — keyword search for an employer returns 3 of their 12 openings |
| major | sql-correctness | `backend/main.py:5007` | `load_only` is silently dropped by `.subquery()`, so the balanced path projects all 33 columns including the 384-dim `embedding_vector` through the window — ~20-40x slower than `sort=newest` |
| major | sql-correctness | `backend/main.py:5012` | The `min_salary` relevance bucket is computed for the window but dropped from the final ordering, so salary-qualifying jobs no longer rank first on the default sort |
| major | scale-performance | `backend/main.py:5007` | Balanced sort's .subquery() discards load_only, streaming embedding_vector (8.4 KB/row) through the window sort |
| major | scale-performance | `backend/main.py:5016` | query.count() re-executes the entire window function a second time on every balanced request |
| major | scale-performance | `backend/models.py:110` | No index supports PARTITION BY company, forcing a full sort of the active set on every request |
| major | frontend-integration | `backend/main.py:5015` | min_salary priority ordering is silently dropped on the new default path, so the Minimum Salary filter appears to do nothing |
| major | test-gaps | `backend/tests/test_job_feed_diversity.py:95` | No test combines `balanced` with `min_salary` — the salary-priority ordering is discarded, so salary-unknown jobs now outrank real matches |
| major | test-gaps | `backend/tests/test_job_feed_diversity.py:52` | No test for blank or fallback company values — every job with an empty company string is treated as one employer and capped at 3 |
| minor | sql-correctness | `frontend/src/components/ScraperTab.jsx:187` | `sort=newest` is unreachable from the UI — the "Newest first" option omits the param and silently returns balanced results |
| minor | frontend-integration | `frontend/src/components/ScraperTab.jsx:187` | Sort By dropdown now mislabels the default and sort=newest is unreachable from any client |
| minor | frontend-integration | `frontend/src/components/HomePage.jsx:83` | total now counts the capped subset, so the public job-count headline and the results label understate the corpus by roughly half |
| minor | create-mcp-docs | `README.md:62` | README's documented manual-install path omits primitives-guide.md, producing a skill with 7 broken references |
| minor | create-mcp-docs | `SKILL.md:9` | SKILL.md:9 claims 'all 10 Smithery quality dimensions' — no table anywhere has 10 rows; the real counts are 18 and 6 |
| minor | create-mcp-docs | `SKILL.md:73` | CREATE path tells the agent to build 1–2 prompts; AUDIT path immediately scores that as a defect against a 3–5 target |
| minor | create-mcp-docs | `references/primitives-guide.md:296` | Installed skill copy is 6 lines behind the repo: primitives-guide.md is missing the completion-matching dogfood paragraph |
| minor | create-mcp-docs | `references/smithery-config.md:82` | Smithery point arithmetic is self-contradictory: the 6 code categories already sum to 100, yet 20 more points are claimed for UI metadata |

<details><summary>Refuted (kept so they are not re-investigated)</summary>

- **Deep pagination pays the full window cost to return zero rows, giving an unauthenticated attacker ~100x request amplific** — REFUTED — measured. The quoted evidence does exist (`backend/main.py:4837-4838`: `page: int = Query(1, ge=1, le=500)`, `per_page: int = Query(20, ge=1, le=100)`), and the endpoint is genuinely unauthenticated behind only a 120-req/min IP limiter (`main.py:4841-4846`). But the failure scenario — deep
- **Nothing tests that `sort=newest` is reachable — the frontend never sends it, so the UI's "Newest first" option silently ** — All quoted evidence is factually present and verified: ScraperTab.jsx:108 defaults sortBy to "newest", :187 omits the sort param exactly when it equals "newest", :866-867 offers only newest/salary, main.py:4835 defaults to "balanced", and git show --stat d139d48 confirms no frontend file was touched
- **Nothing pins the ordering of the balanced result — only per-company counts are asserted** — Quoted evidence is accurate: backend/tests/test_job_feed_diversity.py:94-98 matches verbatim, and the file's only assertions (line 91 `companies.count(SPAM_COMPANY) <= config.JOBS_MAX_PER_COMPANY`, line 98 set-difference) are indeed order-insensitive. But the finding is refuted on three grounds.

(1
- **`sort=salary` is untested — the only sort mode whose ordering expression survives the balanced branch unexercised** — REFUTED — the central factual claim is false. `sort=salary` IS covered by an automated test; the reviewer just searched only one file (backend/tests/test_job_feed_diversity.py) and missed backend/tests/test_job_server_filters.py.

backend/tests/test_job_server_filters.py:89-103 `test_multiple_locati
- **Fixture timestamps are all distinct to the microsecond; production rows are heavily tied on date-only values, so the tie** — REFUTED — the finding explicitly self-refutes and misquotes the file it cites.

1. Self-refutation. The finding's own failure_scenario concludes: "I built a 40-row fixture where every row shares one posted_at_sort and confirmed the current code is stable (zero duplicates across three pages), so this
- **Part A verdict — resources, resource templates, and prompts are all COVERED to tool-level depth (premise of the question** — Refuted as a defect, though the evidence itself is accurate.

I verified every cited line in /Users/koohaoming/dev/create-mcp:
- references/primitives-guide.md:25-121 — `## Static resources` (L25) with `### When the heuristic fires` (L27), `### TypeScript SDK v1` (L42), `### Python FastMCP v1` (L70)
- **AUDIT Step 3 fix order and Step 4 report template have no resource / template / completions entries — the audit detects ** — Quoted text verifies verbatim (SKILL.md:154-157, 165-173, 236, 238-253, 267-276), but the conclusion does not follow.

1) Scope comes from the Step 3 header, not the list: SKILL.md:234 = "### Step 3: Fix everything in one pass". SKILL.md:236 explicitly labels the 15 items as "Priority order (highest
- **Phase 2 Design requires a tool list and a discovery-surface plan as written deliverables, but no resource / template / U** — The finding's quotes are all accurate (SKILL.md:58-62/64/71/73/75/85, primitives-guide.md:95-104 and :348-359, typescript-boilerplate.md:248), but the failure scenario is refuted on three independent grounds.

(1) The URI-scheme decision is not deferred to Phase 3. references/primitives-guide.md:3-5

</details>

## Over-engineering, bugs, dedup

23 confirmed, 9 refuted.

| Severity | Area | Location | Finding |
|---|---|---|---|
| blocker | data-dedup | `/Users/koohaoming/dev/job-hunter-sg/backend/seed_jobs.py:106` | _crawl_semantic_key includes posted_date, which is exactly the field that changes on a repost |
| major | bug | `backend/sanitizer.py:46` | sanitize_job() strips every newline from job descriptions, breaking bullet extraction corpus-wide |
| major | bug | `backend/job_precompute.py:111` | Eight sector keywords with a trailing space can never match — _normalize_match_text emits a double space |
| major | bug | `backend/jd_preparser.py:41` | "bonus" as a preferred-section marker misfiles almost every skill in JDs that mention pay in the intro |
| major | bug | `backend/backfill_enrichment.py:271` | backfill_summaries loops forever on a job whose summary generation keeps raising |
| major | duplication | `shared/resume-classification.json:194` | Seven divergent bullet-marker character classes; the shared config's bullet_markers is read by nothing |
| major | duplication | `backend/resume_scorer.py:81` | resume_scorer.STANDARD_SECTIONS is a 26-item subset of the shared section_headings list |
| major | dead-code | `backend/recruitment_team/assessment_contracts.py:208` | execution_policy advertises five knobs the live runner does not enforce, and the UI prints them as fact |
| minor | dead-code | `backend/main.py:1238` | Entire on-demand JD enrichment subsystem is dead code, including a ThreadPoolExecutor that never receives work |
| minor | over-engineering | `backend/main.py:317` | In-memory power-match cache duplicates the DB snapshot it sits in front of |
| minor | bug | `backend/scraper.py:979` | Dedup merge mutates Job.source, which silently changes the computed dedup_key |
| minor | duplication | `frontend/src/lib/resumeHelpers.jsx:280` | Frontend re-implements the backend resume parser, including byte-identical regexes not sourced from the shared config |
| minor | duplication | `frontend/src/lib/resumeHelpers.jsx:140` | getResumeSectionKey hand-ports classify_section_heading's fallback and disagrees with it on languages, career_break and objective |
| minor | data-dedup | `frontend/src/lib/resumeConstants.js:220` | Action-verb and avoided-phrase word lists maintained twice and already out of sync |
| minor | dead-code | `backend/recruitment_team/candidate_profile_evaluator.py:235` | candidate_profile_evaluator.py is an orphan module: only its own test imports it |
| minor | dead-code | `backend/recruitment_team/prompts/target_assessment.py:10` | Two of three target-assessment system prompts are unreferenced dead text |
| minor | dead-code | `backend/resume_agent/tools.py:201` | resume_agent tools score_resume / extract_skills / analyze_ats_fit are never bound to any live agent |
| minor | dead-code | `backend/recruitment_team/open_agent/runner.py:281` | TargetAssessmentResult.correction is hardcoded None, keeping a dead DB column, telemetry field and UI branch alive |
| minor | bug | `frontend/src/components/ScraperTab.jsx:156` | loadJobs has no request sequencing: a slow response clobbers newer results |
| minor | dead-code | `frontend/src/components/ResumeTab.jsx:1202` | All nine rate-limit branches are unreachable — apiFetch never puts "429" in the message |
| minor | bug | `frontend/src/components/RecruitmentTeamPanel.jsx:150` | refreshThread's `cancelled` flag only guards setError, so a stale thread refresh resurrects a discarded conversation |
| minor | dead-code | `frontend/src/components/ResumeTab.jsx:304` | workspaceView state is write-only — the setter is never called, so every guard it feeds is always true |
| minor | dead-code | `frontend/src/components/ResumeTab.jsx:1131` | Seven `resp.ok` checks after apiFetch are unreachable, and the real failure path is a silent empty catch |

<details><summary>Refuted (kept so they are not re-investigated)</summary>

- **21 route handlers take `body: dict` instead of a Pydantic model** — The quoted lines exist as claimed (`body: dict` at all 21 listed handler lines; the create_story snippet at backend/main.py:6111 is verbatim; siblings use models at backend/main.py:5897 and 7265, not the cited 5899/7263). But the finding is not a defect.

(1) The "duplication" kind fails: the 21 han
- **apply_company_taxonomy overwrites `sector` with an SSIC label from a disjoint vocabulary, splitting the sector facet** — The quoted code is real but the defect framing is not.

Confirmed facts:
- backend/company_taxonomy.py:346-348 does exactly what is quoted: `section = ssic_section_from_code(match.company_ssic_code)` / `if section:` / `job_data["sector"] = section`, and the same overwrite exists on the already-match
- **dedup_key is keyed on MCF's per-posting UUID, so every repost is a brand-new job row** — REFUTED on three independent grounds.

1) The claim's central assertion — "2,371 exact (company,title,description) repeats that no code path collapses" — is false. Two code paths collapse them. backend/seed_jobs.py:94-112 defines `_crawl_semantic_key(job)`, docstring "Collapse same-content source sp
- **Content dedup exists only in seed_jobs.py; both main.py write paths insert duplicates unchecked** — REFUTED. The two code snippets are quoted accurately, but every load-bearing part of the claim — the causal attribution, the magnitude, the reachability, and the impact — fails on inspection.

**What is actually true:** `grep -n "semantic_key\|_crawl_semantic" backend/main.py` does return nothing, a
- **Retitled clones: same company posts one identical description under dozens of different titles, and nothing groups by de** — REFUTED — not a bug. The line quoted is real, but the claim's supporting evidence is factually wrong in one place and its prescribed fix would delete real job postings.

1. The cited line exists, but the behaviour is deliberate, documented, and test-locked.
`/Users/koohaoming/dev/job-hunter-sg/backe
- **Cross-source dedup is structurally impossible: `source` is inside every dedup_key branch, making the aggregator's source** — REFUTED. The quoted lines all exist (scraper.py:137, 142, 145-148, 972-982, 1001, 18; job_store.py:24-25, 36), but the claim misreads intent, reachability, and blast radius.

1) Source-scoping is deliberate and documented, not a broken intent. scraper.py:133 says `"""Generate a source-aware key that
- **The per-company feed cap suppresses 2.25 genuine listings for every duplicate it hides** — REFUTED. The quoted lines exist (main.py:5009-5033, config.py:210) but the claim misreads them on five counts.

1. Demotion, not suppression. main.py:5028-5030 appends a `case(...)` to `balanced_ordering`, and main.py:5035-5042 pages over the whole `ranked` subquery. `total = query.count()` at main.
- **resume_scorer._NORMALIZED_SECTION_KEYS is a stale 24-entry copy of SHARED_KEY_MAP with one divergent value** — The quoted lines exist and the 24-key/one-divergent-value diff is arithmetically correct, but the finding's substance is refuted on three counts.

(1) The dict is not a deletable shadow. Besides the lookup at backend/resume_scorer.py:253, it is used as a set at backend/resume_scorer.py:276 (`if rema
- **AnalyticsTab loadData has the same unguarded overwrite race as ScraperTab** — Evidence quoted is verbatim accurate (AnalyticsTab.jsx:217-245: six filter deps, two parallel apiFetch calls, no AbortController/sequence guard, setLoading(false) at :242 outside a finally), and the component is mounted (App.jsx:492). But the claimed failure mechanism is guarded by the render tree.

</details>

## Recruitment Team UX

22 confirmed, 3 refuted.

| Severity | Area | Location | Finding |
|---|---|---|---|
| major | first-run | `frontend/src/components/RecruitmentTeamPanel.jsx:386` | New user with no resume hits a dead end: the hard dependency is only revealed as a post-submit error string |
| major | first-run | `frontend/src/components/RecruitmentTeamPanel.jsx:407` | First-run empty state explains nothing: no description of what the team does, no suggested prompt, no warning that runs take minutes |
| major | first-run | `frontend/src/components/RecruitmentTeamPanel.jsx:167` | There is no thread list at all - the backend returns full conversation summaries and the frontend throws all but one ID away |
| major | long-run-feedback | `frontend/src/lib/recruitmentTeamApi.js:51` | A dropped SSE stream leaves the UI permanently dead while the run keeps burning minutes server-side |
| major | long-run-feedback | `frontend/src/App.jsx:471` | Switching tabs abandons an in-flight multi-minute run; coming back shows an idle panel and an armed duplicate-run button |
| major | long-run-feedback | `backend/recruitment_team/open_agent/runner.py:272` | The mandatory judge phase emits zero progress events, and the SSE stream has no heartbeat — a silent gap long enough for a proxy to kill the connection |
| major | long-run-feedback | `frontend/src/components/RecruitmentTeamPanel.jsx:846` | All coordinator progress history is discarded — only the single newest coordinator event is ever rendered |
| major | hitl-pause | `/Users/koohaoming/dev/job-hunter-sg/frontend/src/components/RecruitmentTeamPanel.jsx:398` | The agent's blocking question renders as an ordinary chat bubble, with no focus, no scroll, and no notification |
| major | hitl-pause | `/Users/koohaoming/dev/job-hunter-sg/frontend/src/components/RecruitmentTeamPanel.jsx:734` | "Run assessment again" stays enabled during a pause and silently destroys the interrupted run |
| major | hitl-pause | `/Users/koohaoming/dev/job-hunter-sg/frontend/src/components/RecruitmentTeamPanel.jsx:417` | After the candidate answers, the UI keeps insisting the run is paused for the entire multi-minute resume |
| major | hitl-pause | `/Users/koohaoming/dev/job-hunter-sg/frontend/src/components/RecruitmentTeamPanel.jsx:167` | Only the single most-recent thread can be reopened, so a paused run is orphaned the moment the user starts a new conversation |
| major | errors-and-edges | `/Users/koohaoming/dev/job-hunter-sg/frontend/src/components/RecruitmentTeamPanel.jsx:734` | A run in flight is unrecoverable across a reload: no polling, no reconnect, and the button invites a duplicate multi-minute run |
| major | errors-and-edges | `/Users/koohaoming/dev/job-hunter-sg/frontend/src/components/RecruitmentTeamPanel.jsx:731` | The 'Run assessment' button stays enabled during the HITL pause and silently destroys the paused run |
| major | errors-and-edges | `/Users/koohaoming/dev/job-hunter-sg/backend/recruitment_team/open_agent/runner.py:231` | A run truncated by the tool-iteration cap is presented as a finished assessment |
| major | errors-and-edges | `/Users/koohaoming/dev/job-hunter-sg/backend/recruitment_team/recruitment_team.py:923` | Proposed resume edits are written to the database and never shown to anyone |
| major | a11y-and-craft | `frontend/src/components/RecruitmentTeamPanel.jsx:844` | The streaming activity feed is not a live region — screen reader users get total silence for the entire multi-minute run |
| major | a11y-and-craft | `frontend/src/components/RecruitmentTeamPanel.jsx:422` | The composer row overflows horizontally on a phone — no wrap, no responsive stacking |
| major | a11y-and-craft | `frontend/src/components/RecruitmentTeamPanel.jsx:858` | Every activity summary and all evidence metadata fails 4.5:1 contrast |
| minor | first-run | `frontend/src/components/RecruitmentTeamPanel.jsx:175` | A failed first turn orphans a half-created thread and the retry silently creates a duplicate |
| minor | long-run-feedback | `frontend/src/components/RecruitmentTeamPanel.jsx:440` | No elapsed time and no expectation-setting anywhere in a multi-minute wait |
| minor | errors-and-edges | `/Users/koohaoming/dev/job-hunter-sg/backend/recruitment_team/activity_stream.py:62` | Every streamed failure collapses into one generic sentence, so 404 / 422 / 503 / quality-blocked are indistinguishable |
| minor | a11y-and-craft | `frontend/src/components/RecruitmentTeamPanel.jsx:846` | All but the newest coordinator event per run is thrown away, so the open-agent tool loop renders as one self-overwriting line |

<details><summary>Refuted (kept so they are not re-investigated)</summary>

- **Nothing on screen distinguishes the 'recruitment team' from a plain chatbot until the user has already selected a target** — REFUTED. The quoted code exists (RecruitmentTeamPanel.jsx:709 `{roleProfile && (`, :717 "Five isolated evidence reviews, synthesis, then an independent quality judgment."), but the claim's central premise — that on first open the surface is "indistinguishable from any chatbot" — is contradicted by a
- **The activity feed styles "paused" with a loading spinner icon, indistinguishable from an unknown state** — The quoted code exists verbatim (RecruitmentTeamPanel.jsx:6-11, default fallback at :14), and the backend does emit status="paused" (runner.py:255-256) which streams into the sidebar. But the claimed user impact — that the pause "reads as ambient in-progress noise" and is "indistinguishable from an
- **The HITL pause — the one moment the agent is blocked on the human — is the least perceivable state in the UI** — REFUTED as framed. The three code facts are individually accurate — the banner at RecruitmentTeamPanel.jsx:417-421 is a plain <p> with no role and sits outside the aria-live="polite" container (opens :394, closes :414); no .focus() exists anywhere in the file (only ResumeTab.jsx:2453 has focus manag

</details>

## Live browser UAT (not found by code review)

Driven through Playwright against a copy of the production-shaped local DB with real
SEA-LION keys. Full journey: sign in → Recruitment Team → message → search → select
target → study resume → run assessment.

| Severity | Location | Finding |
|---|---|---|
| major | `RecruitmentTeamPanel.jsx:445` | "Search jobs" is `disabled={busy \|\| !message.trim()}` and shares the composer with "Send", so it is greyed out at exactly the moment a user would reach for it. The staged journey is undiscoverable. |
| major | `RecruitmentTeamPanel.jsx` job cards | "Select target" renders fully enabled but 422s with "build the candidate evidence profile before selecting a target job". The required ordering is only discoverable by failing. |
| major | activity feed | Those two 422s are permanently recorded as `coordinator \| failed \| The coordinator could not complete this turn.` A user ordering mistake is presented as the AI team failing. |
| major | `main.py:430` `_idle_summary_filler` | ~95–100 SEA-LION calls per 10 minutes sustained for 5+ hours with zero user activity (~570/hour). On the free tier this saturates quota; produced 10 `ConnectionError`s and a `[CRITICAL] 5 consecutive failures` health alert. |
| minor | `HomePage.jsx:82` | `/api/jobs` is fetched 4× on a single landing-page load. |
| minor | landing page stats strip | Renders `0+ Job Listings / 0 Job Sources / 0+ Skills Tracked / 0h` while the cards above correctly read 16,390+. |
| minor | candidate profile panel | Leaks infrastructure detail to the job seeker: `aisingapore/Qwen-SEA-LION-v4.5-27B-IT · 300s timeout · 2 validation attempts · 2 transport retries`. |

### What worked well

- The HITL interrupt fired correctly and staged its work: artifact `status=paused`,
  `specialist_runs=[]`, with completed work preserved in `pending_specialist_runs`.
- The blocking question was high quality — it caught that the candidate resume is
  cloud-native (Kubernetes/Terraform/AWS) while the target Platform Operations Engineer
  role wants traditional on-prem infrastructure, and asked for specific evidence.
- Candidate Evidence Profile produced 40 evidence-cited fields with verbatim resume quotes.
- Persona sub-agent activity streams correctly (`skeptic submitted its assessment`,
  `hiring_manager called write_todos`). It is the *coordinator* lane that collapses to a
  single self-overwriting line.

### Corrections made during the audit

- "Job discovery ignored the stated public-sector constraint" — **not proven**. The local
  corpus is 100% MyCareersFuture with zero CareersGov rows, so there were no public-sector
  postings to return. The real defect is that the constraint was unsatisfiable and the UI
  never said so; it reported "I found 7 current, source-backed job matches" as if satisfied.
- "Target selection fails silently" — **wrong**. The error is rendered; an earlier snapshot
  was scoped to the composer and cut it off.
- "~500 model calls for one assessment" — **wrong**. That rate is the idle summary filler
  running constantly, not the assessment.
